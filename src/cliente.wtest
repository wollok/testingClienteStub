import cliente.*

test "el saldo del cliente de una sola factura de prueba es el saldo de esa factura" {
	const cliente = new Cliente()
	cliente.agregarFactura(new FacturaPrueba())
	assert.equals(100, cliente.saldo())
}